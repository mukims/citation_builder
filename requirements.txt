# pip equivalent of environment.yml, for non-conda setups.
#
#   python -m pip install -r requirements.txt
#
# See environment.yml for why the first two constraints exist. detectron2 is
# not listed: it is not on PyPI and must be built against your torch/CUDA.
#     python -m pip install 'git+https://github.com/facebookresearch/detectron2.git'

# Constraint fixes — see environment.yml
attrs>=22.2.0
httpx>=0.27

# Retrieval and storage
chromadb~=1.4.1
rank-bm25~=0.2.2
numpy~=2.2.6

# Local models
ollama~=0.6.1
langchain-ollama~=1.0.1
langchain-experimental~=0.4.1

# PDF parsing and layout detection
pymupdf~=1.26.7
opencv-python>=4.8
layoutparser~=0.3.4

# Orchestration and I/O
watchdog~=6.0.0
requests~=2.32
tqdm~=4.67
